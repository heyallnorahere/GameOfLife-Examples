<Project>

    <PropertyGroup>
        <OutDir>$(MSBuildThisFileDirectory)\..\bin</OutDir>
        <LangVersion>latest</LangVersion>
        <Nullable>enable</Nullable>
    </PropertyGroup>

</Project>